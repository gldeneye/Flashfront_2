CREATE TABLE USER(
    ID INT PRIMARY KEY,
    USERNAME VARCHAR(25),
    PASSWORD VARCHAR(50)
)

CREATE TABLE USER(
    ID INT PRIMARY KEY,
    USERNAME VARCHAR(25),
    PASSWORD VARCHAR(50)
)



CREATE TABLE THREAD(
    ID INT PRIMARY KEY,
    NAME VARCHAR(50),
    COMMENTS VARCHAR(500)
)


